# Handoff — Experiment-AI-CMS (AI Content Studio)

> **Latest handoff (2026-06-08). Supersedes `docs/handoff-2026-06-07.md` for current state** — read this first. The 06-07 doc has the deeper AI-layout narrative; 06-05 has the publish-flow/architecture narrative.

**Working dir:** `/Users/nickzaremba/Documents/certainly/iwbi/Experiment-AI-CMS`
**Repo:** `NFZaremba/experiment-ai-cms` (SSH) · **Live:** https://content-studio-ai.netlify.app · **Branch:** `main` (solo repo; commit + deploy from main). **`main` is pushed at `24ee239`.**

A Storyblok-style visual + AI content editor for a marketing landing site. Non-technical editors change text/richtext/links/images/**layout**/**order** and ship via a per-page GitHub PR + Netlify deploy preview, never touching code.

## ⭐ START HERE (next build, approved 2026-06-08)
**Replace the iframe Studio shell with an in-place "admin-bar" editor.** Full approved plan:
`docs/superpowers/plans/2026-06-08-in-place-editor.md` — read it; it has the architecture, the
exact file create/edit/delete list, build order, and verification.

One-line: a floating ✦ pencil appears bottom-right on any *registered* page for authed studio
users; clicking it turns *that page* into the editor in place (same URL + `?edit=1`), chrome via a
body-root portal — no iframe, no postMessage. Retire `app/studio/page.tsx` (shell) +
`EditModeBridge` + `lib/studio/messages.ts`; reuse the draft store, `FloatingChatPanel`, all
`/api/studio/*` routes, and existing auth.

**Execution guidance (important):** do this **inline in one continuous context**, NOT subagent-
driven. It's a faithful, tightly-coupled refactor (the new overlay must reproduce the shell's
exact handlers before the shell can be deleted) — one mind holding the whole picture gives the best
result. (Subagents were great for the *reorder* feature because that decomposed into independent
units; this doesn't.) Run `full-review` at the end before presenting; commit/push only when asked.

## What shipped this session (all on `main`, pushed at `24ee239`)
1. **`/about` page — AI-layout test surface** (`ed2dbfb`). Clean, animation-free copy of the WELL
   "About Us / People-First Places" page from design tokens; 7 section components in
   `components/about/`; 7 constrained layout variants wired into `lib/content/layout-vocab.ts`
   (hero/intro/solutions/report/sum/team/stats). Registered like page-2 (`pages.ts` +
   `getAboutContent`). Avatars are gray initials placeholders (intentional).
2. **Preview-poll bug fix** (`2d04192`). The deploy-preview banner got stuck on "Building…" when a
   new page's first (cold) Netlify build outran the ~3.25-min poll budget. Now: shows the preview
   link while building, ~6-min budget, graceful "open preview / publish anyway" fallback on exhaust.
3. **Drag-and-drop collection reordering** (`aa5a369`→`24ee239`, ~13 commits). Edit-mode drag
   handle (keyboard-accessible, @dnd-kit) on About's `solutions.items` + `team.members`; live
   reorder, composes with the column-count layout variant, publishes via the per-page PR flow.
   Design + plan: `docs/superpowers/specs/2026-06-08-drag-reorder-collections-design.md` +
   `docs/superpowers/plans/2026-06-08-drag-reorder-collections.md`. A `full-review` found + we fixed
   a CRITICAL (a crafted/replayed `order` could duplicate content past the Zod allowlist) — now
   `reorderById` dedupes (always a true permutation) + changeset rejects id-less array targets +
   schema constrains ids to non-numeric slugs (`tests/reorder-guard.test.ts`).

## Read these (don't duplicate — authoritative)
- **In-place editor plan (the next build):** `docs/superpowers/plans/2026-06-08-in-place-editor.md`
- **Reorder design + plan (shipped):** `docs/superpowers/specs/2026-06-08-drag-reorder-collections-design.md`, `docs/superpowers/plans/2026-06-08-drag-reorder-collections.md`
- **Multi-page / publish spine:** `docs/multi-page-publish_checkpoint.md` (NB: its §6 "shell page switcher" is the thing the in-place build retires; the per-page-PR spine it describes is kept).
- **Git is the state:** `git log --oneline -20`. `main` pushed at `24ee239`.

## Gotchas the next agent MUST know
- **Dev server: port 3002** (`npm run dev`). After big changes HMR goes stale → **restart** before browser-verifying (bit us repeatedly). Log: `/tmp/devserver.log`.
- **`tsc --noEmit` has pre-existing noise** (`@syscore/ui-library` internals + youtube-player `YT` globals). Filter to changed files; **`npm run build` is the real gate.**
- **tsx data-layer tests need `--conditions react-server`**: `npx tsx --conditions react-server tests/<f>.test.ts`. Without it, `import "server-only"` (transitively via `changeset.ts` → `sanitize-server.ts`) throws in plain Node.
- **Playwright** is imported by absolute path from the sibling beta repo: `file:///Users/nickzaremba/Documents/certainly/iwbi/onewell-standard-frontend/node_modules/playwright/index.mjs`. `localStorage.clear()` first; stub `/api/studio/preview` + `/api/studio/publish` to avoid real PRs. **For the in-place editor, Playwright drives the real page directly (no iframe) — drop the cross-frame `page.evaluate`/`frameLocator` dance; it's much simpler now.** (For drag, a real pointer-drag with computed coords works; keyboard input to the @dnd-kit sensor was unreliable — see `tests/reorder.e2e.mjs`.)
- **Auth:** `studio_auth` is an **httpOnly** cookie (not client-readable) → gate the pencil by passing a server-computed `authed` boolean from `app/layout.tsx` (server component) into the client mount. `isStudioAuthed()` (`lib/studio/auth.ts`) returns **true in dev when `STUDIO_AUTH_TOKEN` is unset** — so to test the *gate* you must set the token + clear the cookie. `StudioLogin` + `app/studio/layout.tsx` are reused.
- **Editor code currently ships to anon visitors** (`EditModeBridge` is statically imported by the 3 public pages). The in-place plan fixes this (centralize + auth-gate + `dynamic({ssr:false})`); verify the public `/about` bundle has no overlay/@dnd-kit chunk afterward.
- **Layout vocab is keyed by path globally**, not by (page, path) — `lib/content/layout-vocab.ts` has a note. Fine today (unique section keys); scope by page if two pages ever share a path.
- **Env for the live demo:** `ANTHROPIC_API_KEY` (AI "Ask AI" box; manual buttons work without it), `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME`/`_UPLOAD_PRESET` (image uploads).

## Working agreements (the user's preferences)
- **Commit only when asked; push only when asked** (push to `main` = prod Netlify deploy). End commit messages with `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **User does all external setup** (accounts/env/deploys); give precise click-by-click steps.
- **Verify with browser-visible Playwright + build + tsc each phase before claiming done.** Lead-architect rigor: lead with options + trade-offs, recommend, flag risk honestly (the user asks good "is this the right approach?" questions — this session covered iframe-vs-in-place, subagents-vs-inline, the AI-vs-buttons value question).
- The user is cost/context-aware — prefer the leaner execution path; this handoff exists so the in-place build runs in a fresh, clean context.

## Suggested skills for next session
- `superpowers:verification-before-completion` before claiming any phase done.
- `full-review` at the end of the in-place build (Tier 3 — new cross-cutting mount, auth-gating, deletes a subsystem).
- `superpowers:systematic-debugging` if the in-place overlay misbehaves (portal/z-index/listener bugs).
- `handoff` again at the end of the next session.
